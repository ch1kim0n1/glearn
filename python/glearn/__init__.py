"""glearn — Meta-learning and reflective layer for the G-Stack.

This Python package is a thin launcher that runs the bundled Node.js CLI
(`glearn`) via the user's local Node.js runtime (Node >= 18 required).
"""

__version__ = "0.5.0"
